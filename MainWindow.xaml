<Window x:Class="PrintManagementSystem_Kylosov.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrintManagementSystem_Kylosov"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Запись" Click="OpenWrite"/>
            <MenuItem Header="Журнал" Click="OpenLog"/>
        </Menu>

        <Frame x:Name="MainFrame" Margin="0,20,0,0">
            <Frame.Template>
                <ControlTemplate TargetType="Frame">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter Content="{TemplateBinding Content}" />
                    </Border>
                </ControlTemplate>
            </Frame.Template>
        </Frame>
    </Grid>
</Window>
